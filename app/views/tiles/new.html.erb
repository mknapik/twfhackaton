<%= form_for(@tile, :url => { :action => :create }, multipart: true, :class => 'form-inline') do |t| %>
<div class="form-group">
    <%= t.label :name %>
    <%= t.text_field :name %>
</div>
<div class="form-group">
    <%= t.label :image %>
    <%= t.file_field :image %>
</div>
<div class="actions">
    <%= t.submit 'Add new tile' %>
</div>
<% end %>
